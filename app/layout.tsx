import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Arjun U - Portfolio",
  description:
    "Personal portfolio website of Arjun U, a software developer specializing in Java, JavaScript, and web technologies.",
    generator: 'Developed by Digital Product Solutions'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <link rel="icon" href="/favicon.ico" sizes="any" />
        <meta name="author" content="Arjun U" />
        <meta
          name="keywords"
          content="Arjun U, developer, portfolio, Java, JavaScript, Spring Boot, web developer, MCA"
        />
      </head>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="dark" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
